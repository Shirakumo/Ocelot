name: Build APK
on: [create, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Build the app
        run: ./gradlew build
      - name: Prepare keys
        run: gpg --quiet --batch --yes --decrpyt --passphrase "$RELEASE_PASSWORD" --output $HOME/shirakumo.jks shirakumo.jks.gpg
        env:
          RELEASE_PASSWORD: ${{ secrets.RELEASE_PASSWORD }}
      - name: Assemble
        run: ./gradlew assembleRelease
        env:
          RELEASE_STORE_FILE: $HOME/shirakumo.jks
          RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_PASSWORD }}
          RELEASE_KEY_ALIAS: shirakumo
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_PASSWORD }}
      - name: Upload artifact
        uses: actions/upload-artifact@v1
        with:
          name: app
          path: app/build/outputs/apk/release/app-release-unsigned.apk
      - name: Create release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: rel-${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false
      - name: Upload release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: app/build/outputs/apk/release/app-release-unsigned.apk
          asset_name: ocelot.apk
          asset_content_type: application/vnd.android.package-archive
